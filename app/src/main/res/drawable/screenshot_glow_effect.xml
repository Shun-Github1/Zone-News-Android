<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow layer - radiates from iPhone frame edge -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="radial"
                android:centerX="0.5"
                android:centerY="0.5"
                android:gradientRadius="100dp"
                android:startColor="#00000000"
                android:centerColor="#4D00A9A9"
                android:endColor="#00000000" />
            <corners android:radius="30dp" />
        </shape>
    </item>
    <!-- Inner transparent area matching iPhone frame size (160dp × 320dp) -->
    <!-- Glow view is 184dp × 344dp, so 12dp inset on all sides matches iPhone frame -->
    <item
        android:left="12dp"
        android:right="12dp"
        android:top="12dp"
        android:bottom="12dp">
        <shape android:shape="rectangle">
            <solid android:color="#00000000" />
            <corners android:radius="30dp" />
        </shape>
    </item>
</layer-list>
