# Copy this file to keystore.properties and fill in the real values to sign release builds.
# keystore.properties is gitignored; never commit real credentials.
# Without keystore.properties present, release builds are produced unsigned.

storeFile=/absolute/path/to/your.keystore
storePassword=
keyAlias=
keyPassword=
